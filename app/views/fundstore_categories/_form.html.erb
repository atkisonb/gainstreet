<%= form_for @fundstore_category, :html => { :class => 'form-horizontal form-model' } do |f| %>
  <div class="form-group">
    <%= f.label :category_heading, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.text_area :category_heading, :class => 'text-field form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :category_subheading, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.text_area :category_subheading, :class => 'text-field form-control' %>
    </div>
  </div>

  <div class="form-group">
    <div class='col-md-offset-2 col-md-10'>
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                  fundstore_categories_path, :class => 'btn btn-default' %>
    </div>
  </div>
<% end %>
