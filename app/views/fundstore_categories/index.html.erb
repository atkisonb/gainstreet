<%- model_class = FundstoreCategory -%>
<div class="page-header">
  <h1><%=t '.title', :default => FundstoreCategory %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th><%= model_class.human_attribute_name(:category_heading) %></th>
      <th><%= model_class.human_attribute_name(:category_subheading) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @fundstore_categories.each do |fundstore_category| %>
      <tr>
        <td><%= fundstore_category.id %></td>
        <td><%= fundstore_category.category_heading %></td>
        <td><%= fundstore_category.category_subheading %></td>
        <td><%=l fundstore_category.created_at, format: :long %></td>
        <td>
          <%= link_to fundstore_category_path(fundstore_category), :class => 'btn btn-xs', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
            <%= glyph 'info-sign' %>
            <%= glyph 'pencil' %>
          <%- end -%>
          <%= link_to fundstore_category_path(fundstore_category), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs', :title => "#{ t('.destroy', :default => t('helpers.links.destroy')) }" do %>
            <%= glyph 'remove' %>
          <%- end -%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_fundstore_category_path,
            :class => 'btn btn-primary' %>
