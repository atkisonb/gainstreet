<%= form_for(@fund_cash) do |f| %>
  <% if @fund_cash.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fund_cash.errors.count, "error") %> prohibited this fund_cash from being saved:</h2>

      <ul>
      <% @fund_cash.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :amount %><br>
    <%= f.text_field :amount %>
  </div>
  <div class="field">
    <%= f.label :fund_id %><br>
    <%= f.text_field :fund_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
