<%- model_class = FundCash -%>
<div class="page-header">
  <h1><%=t '.title', :default => FundCash %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th><%= model_class.human_attribute_name(:amount) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @fund_cashes.each do |fund_cash| %>
      <tr>
        <td><%= fund_cash.id %></td>
        <td><%= fund_cash.amount %></td>
        <td><%=l fund_cash.created_at, format: :long %></td>
        <td>
          <%= link_to fund_cash_path(fund_cash), :class => 'btn btn-xs', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
            <%= glyph 'info-sign' %>
            <%= glyph 'pencil' %>
          <%- end -%>
          <%= link_to fund_cash_path(fund_cash), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs', :title => "#{ t('.destroy', :default => t('helpers.links.destroy')) }" do %>
            <%= glyph 'remove' %>
          <%- end -%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_fund_cash_path,
            :class => 'btn btn-primary' %>
